services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: statement-analyzer:latest
    container_name: statement-analyzer
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
